project_name: chip8-wails
builds:
  - id: wails
    main: .
    goos: [linux, windows, darwin]
    goarch: [amd64]
nfpms:
  - id: linuxpkgs
    formats: [deb, rpm]
    maintainer: "your_email@example.com"
    bindir: /usr/bin
release:
  github:
    owner: bethropolis
    name: chip8
